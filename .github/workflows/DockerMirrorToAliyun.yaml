name: Sync Multi-Arch Docker Images to Alibaba Cloud

on:
  workflow_dispatch:
  schedule:
    - cron: '0 2 * * *'

jobs:
  sync-images:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout
      uses: actions/checkout@v3

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v2

    - name: Login to Alibaba Cloud Container Registry
      uses: docker/login-action@v2
      with:
        registry: registry.cn-hangzhou.aliyuncs.com
        username: ${{ secrets.ALIYUN_USERNAME }}
        password: ${{ secrets.ALIYUN_PASSWORD }}

    - name: Install dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y jq

    - name: Process images
      run: |
        echo "=== Starting image processing ==="
        
        # Read the JSON file directly in the shell
        count=$(jq 'length' images.json)
        echo "Found $count images to process"
        
        # Simple loop with proper error handling
        for i in $(seq 0 $((count-1))); do
          echo "=== Processing image $((i+1)) ==="
          
          # Extract values one by one with error checking
          source=$(jq -r ".[$i].source" images.json)
          target=$(jq -r ".[$i].target" images.json)
          tag_count=$(jq ".[$i].tags | length" images.json)
          
          echo "Source: $source"
          echo "Target: $target"
          echo "Tag count: $tag_count"
          
          if [ "$source" = "null" ] || [ "$target" = "null" ]; then
            echo "❌ Invalid data at index $i"
            continue
          fi
          
          # Pull images
          echo "📥 Pulling $source for amd64..."
          docker pull --platform linux/amd64 "$source"
          
          echo "📥 Pulling $source for arm64..."
          docker pull --platform linux/arm64 "$source"
          
          # Process tags
          for j in $(seq 0 $((tag_count-1))); do
            tag=$(jq -r ".[$i].tags[$j]" images.json)
            echo "🏷️  Processing tag: $tag"
            
            full_target="registry.cn-hangzhou.aliyuncs.com/${{ secrets.ALIYUN_NAMESPACE }}/$target:$tag"
            echo "📦 Tagging as $full_target"
            docker tag "$source" "$full_target"
            
            echo "📤 Pushing amd64 version..."
            docker push "$full_target" --platform linux/amd64
            
            echo "📤 Pushing arm64 version..."
            docker push "$full_target" --platform linux/arm64
          done
        done

    - name: Cleanup
      if: always()
      run: |
        echo "🧹 Cleaning up..."
        # Add cleanup commands here if needed
